mixin tabs(active='', ...args)
  ul.nav
    - for (let i = 0; i < args.length; i+= 2) {
      li.nav-item
        a.nav-link.btn.btn-primary.mr-4(class=(active === args[i] ? 'active' : ''), href=args[i+1])= args[i]
    - }
